spring:
  application:
    name: nblog
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/nblog?useSSL=false&serverTimezone=UTC
server:
  port: 8095
