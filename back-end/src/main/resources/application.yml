spring:
  application:
    name: nblog
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://127.0.0.1:3306/nblog?useSSL=false&serverTimezone=UTC
server:
  port: 8095
  servlet:
    context-path: /api
#mybatis-plus:
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
logging:
  level:
    com.noodb.blog.mapper: debug
